---
title: ImapStatusCode
second_title: Referencia de la API de Aspose.Email para .NET
description: Representa las respuestas de estado.
type: docs
weight: 16500
url: /es/net/aspose.email.clients.imap/imapstatuscode/
---
## ImapStatusCode enumeration

Representa las respuestas de estado.

```csharp
[Flags]
public enum ImapStatusCode
```

### Valores

| Nombre | Valor | Descripción |
| --- | --- | --- |
| NotDefined | `0` | No definido |
| Ok | `1` | La respuesta OK indica un mensaje de información del servidor. Cuando se etiqueta, indica que se completó con éxito el comando asociado. El texto legible por humanos PUEDE presentarse al usuario como un mensaje de información. El formulario sin etiquetar indica un mensaje de solo información; la naturaleza de la información PUEDE indicarse mediante un código de respuesta. |
| No | `2` | La respuesta NO indica un mensaje de error operativo del servidor. Cuando se etiqueta, indica que no se completó correctamente el comando asociado. El formulario sin etiquetar indica una advertencia; el comando aún puede completarse con éxito. El texto legible por humanos describe la condición. |
| Bad | `4` | La respuesta BAD indica un mensaje de error del servidor. Cuando se etiqueta, informa un error a nivel de protocolo en el comando del cliente; la etiqueta indica el comando que provocó el error. El formulario sin etiquetar indica un error a nivel de protocolo para el cual no se puede determinar el comando asociado; también puede indicar una falla interna del servidor. El texto legible por humanos describe la condición. |
| Preauth | `8` | La respuesta PREAUTH siempre está sin etiquetar y es uno de los tres saludos posibles al inicio de la conexión. Indica que la conexión ya ha sido autenticada por medios externos y por lo tanto no se necesita comando LOGIN. |
| Bye | `10` | La respuesta BYE siempre está sin etiquetar e indica que el servidor está a punto de cerrar la conexión. El texto legible por humanos PUEDE mostrarse al usuario en un informe de estado por parte del cliente. La diferencia entre un BYE que ocurre como parte de una secuencia de LOGOUT normal (el primer caso) y un BYE que ocurre debido a una falla (los otros tres casos) es que la conexión se cierra inmediatamente en caso de falla. |
| Alert | `20` | El texto legible por humanos contiene una alerta especial que DEBE presentarse al usuario de manera que llame la atención del usuario sobre el mensaje. |
| BadCharset | `40` | Opcionalmente seguido de una lista de juegos de caracteres entre paréntesis. Una BÚSQUEDA falló porque el juego de caracteres dado no es compatible con esta implementación. |
| Capability | `80` | Seguido de una lista de capacidades. |
| Parse | `100` | El texto legible por humanos representa un error al analizar el encabezado [RFC-2822] o los encabezados [MIME-IMB] de un mensaje en el buzón . |
| PermanentFlags | `200` | Seguido de una lista de banderas entre paréntesis, indica cuál de las banderas conocidas el cliente puede cambiar permanentemente. |
| ReadOnly | `400` | El buzón está seleccionado de solo lectura. |
| ReadWrite | `800` | El buzón está seleccionado lectura-escritura. |
| TryCreate | `1000` | Un intento de AÑADIR o COPIAR está fallando porque el buzón de destino no existe. |
| UIDNext | `2000` | Indica el siguiente valor de identificador único. |
| UIDValidity | `4000` | Indica el valor de validez del identificador único. |
| Unseen | `8000` | Indica el número del primer mensaje sin el indicador \Seen establecido. |
| Unsolicited | `10000` | Respuestas de estado sin etiquetar. |

### Ver también

* espacio de nombres [Aspose.Email.Clients.Imap](../../aspose.email.clients.imap)
* asamblea [Aspose.Email](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Email.dll -->

---
title: ImapStatusCode
second_title: Aspose.Email per riferimento all'API .NET
description: Rappresenta le risposte di stato.
type: docs
weight: 16500
url: /it/net/aspose.email.clients.imap/imapstatuscode/
---
## ImapStatusCode enumeration

Rappresenta le risposte di stato.

```csharp
[Flags]
public enum ImapStatusCode
```

### I valori

| Nome | Valore | Descrizione |
| --- | --- | --- |
| NotDefined | `0` | Non definito |
| Ok | `1` | La risposta OK indica un messaggio informativo dal server. Quando contrassegnato, indica il completamento con successo del comando associato. Il testo leggibile PUÒ essere presentato all'utente come messaggio informativo. Il modulo senza tag indica un messaggio solo informativo; la natura delle informazioni PUÒ essere indicata da un codice di risposta. |
| No | `2` | La risposta NO indica un messaggio di errore operativo dal server. Quando contrassegnato, indica il completamento non riuscito del comando associato. Il modulo senza tag indica un avviso; il comando può ancora essere completato correttamente. Il testo leggibile descrive la condizione. |
| Bad | `4` | La risposta BAD indica un messaggio di errore dal server. Quando contrassegnato, segnala un errore a livello di protocollo nel comando del client; il tag indica il comando che ha causato l'errore. Il modulo senza tag indica un errore a livello di protocollo per il quale non è possibile determinare il comando associato; può anche indicare un errore interno del server. Il testo leggibile descrive la condizione. |
| Preauth | `8` | La risposta PREAUTH è sempre senza tag ed è uno dei tre possibili saluti all'avvio della connessione. Indica che la connessione è già stata autenticata con mezzi esterni e quindi non è necessario alcun comando LOGIN. |
| Bye | `10` | La risposta BYE è sempre senza tag e indica che il server sta per chiudere la connessione. Il testo leggibile PUÒ essere visualizzato all'utente in un rapporto di stato dal client. La differenza tra un BYE che si verifica come parte di una normale sequenza di LOGOUT (il primo caso) e un BYE che si verifica a causa di un guasto (gli altri tre casi) è che la connessione si chiude immediatamente in caso di guasto. |
| Alert | `20` | Il testo leggibile contiene un avviso speciale che DEVE essere presentato all'utente in un modo che richiami l'attenzione dell'utente sul messaggio. |
| BadCharset | `40` | Facoltativamente seguito da un elenco di set di caratteri tra parentesi. A SEARCH non è riuscito perché il set di caratteri specificato non è supportato da questa implementazione. |
| Capability | `80` | Seguito da un elenco di capacità. |
| Parse | `100` | Il testo leggibile rappresenta un errore nell'analisi dell'intestazione [RFC-2822] o delle intestazioni [MIME-IMB] di un messaggio nella casella di posta . |
| PermanentFlags | `200` | Seguito da un elenco di flag tra parentesi, indica quale dei i flag conosciuti il client può modificare in modo permanente. |
| ReadOnly | `400` | La casella di posta è selezionata in sola lettura. |
| ReadWrite | `800` | La casella di posta è selezionata in lettura-scrittura. |
| TryCreate | `1000` | Un tentativo APPEND o COPY non riesce perché la cassetta postale di destinazione non esiste. |
| UIDNext | `2000` | Indica il valore dell'identificatore univoco successivo. |
| UIDValidity | `4000` | Indica il valore di validità dell'identificatore univoco. |
| Unseen | `8000` | Indica il numero del primo messaggio senza il flag \Seen impostato. |
| Unsolicited | `10000` | Risposte di stato senza tag. |

### Guarda anche

* spazio dei nomi [Aspose.Email.Clients.Imap](../../aspose.email.clients.imap)
* assemblea [Aspose.Email](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Email.dll -->
